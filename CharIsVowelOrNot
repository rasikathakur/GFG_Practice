// Given a English alphabet c, Write a program to check whether a character is a vowel or not.
class Solution {
    String isVowel(char c) {
        char mychar = Character.toLowerCase(c);
        if(mychar != 'a' && mychar != 'e' && mychar != 'i' && mychar != 'o' && mychar != 'u'){
            return "NO";
        }else{
            return "YES";
        }
    }
}

//output

Input:
c = 'a'
Output:
YES
Explanation:
'a' is a vowel.
